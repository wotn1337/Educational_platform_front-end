const initState = {
	loggedIn: false,
	active: false
};

const navbarReducer = (state = initState, action) => {
	switch (action.type) {

		default:
			return state;
	}
};


export default navbarReducer;