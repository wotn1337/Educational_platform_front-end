const initState = {
	task: "В этой игре нужно найти каждой картинке свою пару. Посмотри внимательно на все карточки и запомни одинаковые.",
};

const pairsReducer = (state = initState, action) => {
	switch (action.type) {
		default:
			return state;
	}
}

export default pairsReducer;